# Overview

This system creates a market primitive for fractional ownership of concentrated liquidity positions.

Users access strategy exposure by minting fungible shares backed by deterministic vault accounting.

Core principles:

- deterministic share accounting
- no background automation dependencies
- explicit and auditable hook/vault boundaries
